# Implementation

This example keeps `implementation/` present to show the intended same-repo lifecycle.

No product code is included in the reference example. The directory exists only to demonstrate where implementation should begin after Gate 2 in a real instance, with `design/07-quality.md` governing the readiness handoff.
